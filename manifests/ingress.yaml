apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deletetask-ingress
  labels:
    app.kubernetes.io/name: deletetask-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: delete-task-api.devopsai.online
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: deletetask-api-svc
            port: 
              number: 80
