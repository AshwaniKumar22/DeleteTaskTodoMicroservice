apiVersion: apps/v1
kind: Deployment
metadata:
  name: deletetask-api
spec:
  selector:
    matchLabels:
      app: deletetask-api
  template:
    metadata:
      labels:
        app: deletetask-api
    spec:
      containers:
      - name: deletetask-api
        image: <Image>
        ports:
        - containerPort: 
